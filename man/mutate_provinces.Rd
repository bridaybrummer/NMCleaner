% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_provinces.R
\name{mutate_provinces}
\alias{mutate_provinces}
\title{Standardise South African province names and codes}
\usage{
mutate_provinces(
  data,
  province_col = "province",
  out_col = "province_standard",
  code_col = "prov",
  keep_raw = TRUE,
  fuzzy = FALSE,
  max_dist = 2,
  dictionary = NULL,
  debug = FALSE
)
}
\arguments{
\item{data}{A data.frame or a character vector of province names/abbreviations.}

\item{province_col}{Name of the column in \code{data} that contains the province
text (only used when \code{data} is a data.frame). Default: "province".}

\item{out_col}{Name of the output column for canonical province names.
Default: "province_standard".}

\item{code_col}{Name of the output column for the short code (e.g. "GP").
Default: "prov".}

\item{keep_raw}{If FALSE, the original \code{province_col} will be removed from
the returned data.frame. Default: TRUE.}
}
\value{
If \code{data} is a data.frame, returns the data.frame with added
\code{out_col} and \code{code_col}. If \code{data} is a character vector, returns a
data.frame with columns \code{input}, \code{province_standard}, and \code{prov}.
}
\description{
This function standardises province inputs (full names or abbreviations)
into canonical province names and the short province code (\code{prov}). It is
robust to differences in case, punctuation and common short forms.
}
\examples{
mutate_provinces(c("EC", "Gauteng", "western cape"))
mutate_provinces(data.frame(province = c("EC", "Eastern Cape")))
}
