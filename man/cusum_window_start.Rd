% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cusum_window_start.R
\name{cusum_window_start}
\alias{cusum_window_start}
\title{Sliding CUSUM window summary}
\usage{
cusum_window_start(
  data,
  window = 180,
  k = 0.5,
  cusum_threshold = 5,
  date_index = "date",
  start_date = NULL
)
}
\arguments{
\item{data}{Data frame containing at least a date column and a count column
named \code{n} (or the column expected by your \code{ears()} implementation).}

\item{window}{Integer window size (number of rows) for the sliding window.}

\item{k}{Numeric CUSUM k parameter (passed to \code{cusum()}). Default 0.5.}

\item{cusum_threshold}{Numeric CUSUM threshold (passed to \code{cusum()}).
Default 5.}

\item{date_index}{Character scalar giving the name of the date column in
\code{data} (default: "date"). The function will coerce this column to Date.}

\item{start_date}{Optional start Date — if provided the function will drop
rows older than \code{start_date - window} before processing.}
}
\value{
A data frame with one row per window containing the final row
variables from the CUSUM output and two helper columns: \code{iteration} and
\code{latest_is_cusums_outbreak}.
}
\description{
Run a sliding-window EARS + CUSUM analysis over a time series and return the
last row from each window. The implementation avoids repeated date parsing
and uses a single-pass lapply to reduce intermediate allocations.
}
\examples{

df <- tibble::tibble(
  date = seq.Date(as.Date('2020-01-01'), by = 1, length.out = 200),
  n = rpois(200, 2)
)
# run a fast sliding analysis


}
